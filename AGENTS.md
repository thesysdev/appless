# Expo version

This project is pinned to **Expo SDK 54** — the version the Play Store build
of Expo Go currently supports. Read the exact versioned docs at
https://docs.expo.dev/versions/v54.0.0/ before writing any code, and do not
bump the SDK without checking what Expo Go on the stores supports.
